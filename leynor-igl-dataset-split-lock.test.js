import test from 'node:test';import assert from 'node:assert/strict';import{createDatasetSplitLock,auditDatasetSplitLock}from'./leynor-igl-dataset-split-lock.js';
const base={splitPlanId:'split-001',version:'1.0.0',sourceAuditFingerprint:'sha256:a',splits:[{role:'development',datasetId:'dev',fingerprint:'sha256:dev',startDate:'2005-01-01',endDate:'2014-12-31',recordIds:['a']},{role:'validation',datasetId:'val',fingerprint:'sha256:val',startDate:'2015-01-01',endDate:'2019-12-31',recordIds:['b']},{role:'locked-test',datasetId:'test',fingerprint:'sha256:test',startDate:'2020-01-01',endDate:'2025-12-31',recordIds:['c'],locked:true,openedAt:null}]};
test('gèle trois jeux strictement séparés',()=>{const p=createDatasetSplitLock(base);assert.equal(p.lockedTestAccessible,false);assert.equal(p.splits[2].role,'locked-test');assert.equal(Object.isFrozen(p),true);});
test('refuse les lignes partagées',()=>{const x=structuredClone(base);x.splits[2].recordIds=['a'];assert.throws(()=>createDatasetSplitLock(x),/must not overlap/);});
test('refuse les périodes qui se chevauchent',()=>{const x=structuredClone(base);x.splits[1].startDate='2014-12-31';assert.throws(()=>createDatasetSplitLock(x),/periods must not overlap/);});
test('refuse une empreinte réutilisée',()=>{const x=structuredClone(base);x.splits[2].fingerprint=x.splits[1].fingerprint;assert.throws(()=>createDatasetSplitLock(x),/fingerprints must be unique/);});
test('refuse un test final ouvert',()=>{const x=structuredClone(base);x.splits[2].openedAt='2026-08-02';assert.throws(()=>createDatasetSplitLock(x),/locked and unopened/);});
test('autorise seulement l’intégration historique',()=>{const a=auditDatasetSplitLock(createDatasetSplitLock(base));assert.equal(a.passed,true);assert.equal(a.eligibleForHistoricalDataIntegration,true);assert.equal(a.readyForExternalValidation,false);assert.equal(a.readyForProduction,false);});

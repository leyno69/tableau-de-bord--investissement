import test from 'node:test';import assert from 'node:assert/strict';import { createProspectiveForecast,evaluateIntervalCoverage,evaluateProbabilityCalibration,settleProspectiveForecast } from './portfolioProbabilityCalibration.js';
test('mesure couverture et incertitude sans fabriquer une validation',()=>{const records=Array.from({length:20},(_,i)=>({outcome:i,intervals:{'0.5':{lower:5,upper:14},'0.9':{lower:1,upper:18}}}));const result=evaluateIntervalCoverage(records);assert.equal(result[0].hits,10);assert.equal(result[1].hits,18);assert.ok(result[0].wilson95.lower<result[0].observedCoverage);});
test('compare le score probabiliste à un taux de base',()=>{const records=[{probability:.8,binaryOutcome:true},{probability:.2,binaryOutcome:false}];const result=evaluateProbabilityCalibration(records,.5);assert.equal(result.comparison.beatsBenchmark,true);});
test('verrouille une prévision prospective avant maturité',()=>{const forecast=createProspectiveForecast({forecastId:'f1',engineCommit:'abc',portfolioId:'p1',asOf:'2026-08-07T00:00:00Z',maturesAt:'2026-11-07T00:00:00Z',horizonMonths:3,probability:.6,intervals:{},assumptionsFingerprint:'def',createdWithoutOutcomeAccess:true});assert.throws(()=>settleProspectiveForecast(forecast,{observedAt:'2026-11-07T00:00:00Z',outcome:1,binaryOutcome:true},'2026-10-01T00:00:00Z'),/non mature/);const settled=settleProspectiveForecast(forecast,{observedAt:'2026-11-07T00:00:00Z',outcome:1,binaryOutcome:true},'2026-11-08T00:00:00Z');assert.equal(settled.forecastFingerprint,forecast.fingerprint);});
